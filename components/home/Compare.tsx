
'use client'
import { ArrowRight, Sparkles } from 'lucide-react'
import { useState } from 'react'
import { Button } from '../buttons'
export function Compare(){const [goal,setGoal]=useState('');const [result,setResult]=useState('');const findMatch=()=>setResult(goal?`Great — we’ll use “${goal}” to find relevant degrees and universities. Our comparison flow is ready to connect to your recommendation API.`:'Tell us a little about your career goal to get started.');return <section id="compare" className="container-page section-space scroll-mt-20"><div className="overflow-hidden rounded-3xl bg-gradient-to-r from-brand-600 via-brand-500 to-orange-500 p-7 text-white shadow-glow sm:p-12"><div className="grid items-center gap-10 lg:grid-cols-[1.2fr_.8fr]"><div><span className="inline-flex items-center gap-2 rounded-full bg-white/15 px-3 py-1.5 text-xs font-bold"><Sparkles size={14}/>AI degree matcher</span><h2 className="mt-5 text-4xl font-extrabold tracking-tight sm:text-5xl">Not sure where to start?</h2><p className="mt-4 max-w-xl leading-7 text-brand-50">Answer one question and use your priorities to create a shortlist of programs worth exploring.</p></div><div className="rounded-2xl bg-white p-5 text-zinc-900 shadow-card"><label className="text-sm font-extrabold">What&apos;s your next career goal?</label><input value={goal} onChange={e=>setGoal(e.target.value)} onKeyDown={e=>e.key==='Enter'&&findMatch()} placeholder="e.g. Move into product management" className="mt-3 w-full rounded-lg border px-3 py-3 text-sm outline-none focus:border-brand-500"/><Button onClick={findMatch} className="mt-3 w-full">Find my path <ArrowRight className="ml-1.5" size={16}/></Button>{result&&<p className="mt-4 rounded-lg bg-brand-50 p-3 text-xs leading-5 text-brand-900">{result}</p>}</div></div></div></section>}
